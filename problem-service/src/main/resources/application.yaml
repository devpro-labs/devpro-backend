spring:
  application:
    name: PROBLEM-SERVICE
#eureka server configure
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost


#port
server:
  port: 8082
